name: Deploy Next.js to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: "24"
          cache: 'npm'
          cache-dependency-path: 'next-docs/package-lock.json'

      - name: Install dependencies
        working-directory: ./next-docs
        run: npm ci

      - name: Install Wrangler CLI
        run: npm install --global wrangler@4

      - name: Cache Next.js build artifacts
        uses: actions/cache@v5.0.5
        with:
          path: next-docs/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('next-docs/package-lock.json') }}-${{ hashFiles('next-docs/**/*.ts', 'next-docs/**/*.tsx', 'next-docs/**/*.mdx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('next-docs/package-lock.json') }}-

      - name: Allocate swap space
        run: |
          sudo fallocate -l 8G /swapfile-new
          sudo chmod 600 /swapfile-new
          sudo mkswap /swapfile-new
          sudo swapon /swapfile-new

      - name: Build Next.js site
        working-directory: ./next-docs
        run: |
          rm -rf out
          npm run build
        env:
          NODE_OPTIONS: "--max_old_space_size=6144"
          GOOGLE_ANALYTICS_ID: ${{ secrets.GOOGLE_ANALYTICS_ID }}
          GOOGLE_SITE_VERIFICATION: ${{ secrets.GOOGLE_SITE_VERIFICATION }}

      - name: Validate Local Links
        working-directory: ./next-docs
        run: |
          # Use linkinator to check internal broken links only without failing the build.
          # We skip all 'http' links to only validate relative/local routing inside 'out'.
          npx linkinator ./out --recurse --skip "^http" || echo "Link validation caught broken links. Check the logs above."

      - name: Upload generated site artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: next-docs-site
          path: next-docs/out
          if-no-files-found: error

      - name: Deploy to Cloudflare Pages
        run: wrangler pages deploy next-docs/out --project-name=zero-to-ai --branch="${GITHUB_REF_NAME}"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
