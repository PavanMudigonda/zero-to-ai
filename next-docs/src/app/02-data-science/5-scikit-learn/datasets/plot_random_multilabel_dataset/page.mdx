---
title: "Plot Random Multilabel Dataset"
---

This example page was normalized to a directory-backed route so Nextra can resolve the `datasets/_meta.ts` entry during static generation.

The notebook asset for this example is not present in the repository, so this page intentionally omits the embedded notebook until the source file is restored.